# 🚀 JMDF公式アカウント（Google / GitHub / Vercel）完全移管ガイド

本ドキュメントは、JMDF（日本改良メダカディーラーズフェデレーション）のHPシステム一式を、**新規に取得する「JMDF公式アカウント」へ安全かつ完全に移管するための作業手順書**です。

---

## 📋 全体スケジュールと移管の順番

最もトラブルが少なく安全な作業順序は以下の通りです。

```
【ステップ1】新規Googleアカウントの作成
      │   （例: jmdf.official@gmail.com）
      ▼
【ステップ2】Googleスプレッドシート＆GASのオーナー移管
      │   （データとAPIプログラムの引っ越し）
      ▼
【ステップ3】新規GitHubアカウントの作成＆リポジトリ移管
      │   （プログラムコード保管場所の引っ越し）
      ▼
【ステップ4】新規Vercelアカウントの作成＆Webサイト公開
      │   （本番HPサーバーの引っ越し）
      ▼
【ステップ5】Apifyアカウントの作成＆APIトークン設定
          （Instagram自動取得の再稼働）
```

---

## 🛠️ ステップ1：新規Googleアカウントの作成

まずは、すべての土台となる **「JMDF公式Googleアカウント」** を1つ作成します。

1. [Google アカウント作成ページ](https://accounts.google.com/signup) へアクセスします。
2. アカウント名を設定して作成します。
   * **メールアドレス例**: `jmdf.official@gmail.com` （または団体独自の代表メールアドレス）
3. ※このメールアドレスを使って、以降のGitHubやVercel、Apifyのアカウントも登録します。

---

## 🛠️ ステップ2：Googleスプレッドシート＆GASの移管

現在の管理用スプレッドシート（`JMDF_加盟店・ニュース管理`）を新規Googleアカウントへ移管します。

### 2-1. スプレッドシートのオーナー権限を譲渡
1. **現在のアカウント** でGoogleドライブを開きます。
2. 管理用スプレッドシート（`JMDF_加盟店・ニュース管理`）を開き、右上の **「共有」** ボタンをクリックします。
3. **新規JMDF Googleアカウント**（例: `jmdf.official@gmail.com`）のメールアドレスを入力し、権限を **「編集者」** として送信します。
4. 再度「共有」を開き、追加した新規アカウントの右側プルダウンから **「オーナー権限の譲渡」** を選択して完了します。

### 2-2. 新規アカウントでGAS WebAppを再公開
1. **新規JMDF Googleアカウント** でスプレッドシートを開きます。
2. 上部メニューの **「拡張機能」>「Apps Script」** をクリックします。
3. 右上の青いボタン **「デプロイ」>「新しいデプロイ」** をクリックします。
4. 歯車アイコンから **「ウェブアプリ」** を選択し、以下の通り設定します：
   * **説明**: `JMDF API 本番公開`
   * **次のユーザーとして実行**: **`自分 (jmdf.official@gmail.com)`**
   * **アクセスできるユーザー**: **`全員`**
5. **「デプロイ」** をクリックし、アクセス承認画面が出たら「詳細」>「安全ではないページへ移動」をクリックして許可します。
6. 発行された **「ウェブアプリのURL」** をコピーしてメモしておきます。
   * 例: `https://script.google.com/macros/s/AKfycb.../exec`
   * 💡 動作確認: 末尾に `?api=data` をつけてブラウザで開き、JSONデータが表示されれば成功です！

---

## 🛠️ ステップ3：新規GitHubアカウントの作成＆リポジトリ移管

プログラムの保管庫（GitHub）を新規アカウントへ移管します。

### 3-1. 新規GitHubアカウントの作成
1. [GitHub](https://github.com/signup) へアクセスします。
2. ステップ1で作成した **JMDF公式Googleメールアドレス** を使って登録します。
3. **ユーザー名例**: `jmdf-official` （または団体名）

### 3-2. リポジトリの所有権を移管（Transfer）
1. **現在のGitHubアカウント（`kotugai-hub`）** でログインします。
2. リポジトリ [`jmdf-vercel`](https://github.com/kotugai-hub/jmdf-vercel) のページを開きます。
3. 上部タブの **「Settings」** をクリックします。
4. 画面の一番下にある **「Danger Zone」** セクションまでスクロールします。
5. **「Transfer ownership」** （所有権の譲渡）ボタンをクリックします。
6. **New owner's username**: 新規GitHubアカウントのユーザー名（例: `jmdf-official`）を入力します。
7. 「I understand, transfer this repository」をクリックして移管リクエストを送信します。
8. **新規GitHubアカウント** にログイン（または届いた確認メール）し、移管承認（**Accept transfer**）をクリックして完了です！

---

## 🛠️ ステップ4：新規Vercelアカウントの作成＆HP公開の移管

本番のWebサーバー（Vercel）を新規アカウントでセットアップします。

### 4-1. 新規Vercelアカウントの作成
1. [Vercel](https://vercel.com/signup) へアクセスします。
2. **「Continue with GitHub」** を選択し、ステップ3で作成した **新規GitHubアカウント（`jmdf-official`）** でログインします。

### 4-2. プロジェクトのインポート（公開設定）
1. Vercelのダッシュボードで、右上の **「Add New...」>「Project」** をクリックします。
2. 移管されたリポジトリ **`jmdf-vercel`** の横にある **「Import」** をクリックします。
3. **Configure Project** 画面が表示されます：
   * **Project Name**: `jmdf-vercel`（そのまま）
   * **Framework Preset**: `Next.js`（自動検出）
4. （推奨）**Environment Variables（環境変数）** の設定：
   * **Key**: `NEXT_PUBLIC_GAS_API_URL`
   * **Value**: ステップ2-2でコピーした新しいGAS WebApp URL（末尾に `?api=data` を付与）
5. 右下の青いボタン **「Deploy」** をクリックします。
6. 1〜2分でビルドが完了し、新しい本番URL（例: `https://jmdf-vercel.vercel.app`）でHPが公開されます！

---

## 🛠️ ステップ5：Apifyアカウントの作成＆Instagram自動取得の再設定

各加盟店のInstagram最新投稿を自動取得するサービス（Apify）のアカウントを作成します。

1. [Apify](https://console.apify.com/sign-up) へアクセスし、JMDF公式メールアドレスで無料登録します。
2. ログイン後、左下のアカウントアイコン > **「Settings」>「Integrations」** タブを開きます。
3. **API tokens** にある **Personal API token** をコピーします。
4. 新規Googleアカウントでスプレッドシートの **「拡張機能」>「Apps Script」** を開きます。
5. 左メニューの **「プロジェクトの設定（歯車アイコン）」** をクリックします。
6. 最下部の **「スクリプト プロパティ」** で「スクリプト プロパティを追加」をクリック：
   * **プロパティ名**: `APIFY_API_TOKEN`
   * **値**: コピーしたApifyのAPIトークンを貼り付けて保存
7. 左メニューの「エディタ（＜＞アイコン）」に戻り、ファイル `fetchApifyInstagram.gs` を開きます。
8. 上部の関数選択で **`setupDailyInstagramTrigger`** を選び、**「実行」** をクリックします。
   * これで、毎日深夜2:00に自動でInstagram投稿を収集するトリガーが設定完了します！

---

## ✅ 移管完了チェックリスト

- [ ] 新規Googleアカウントでスプレッドシートの編集・保存ができる
- [ ] 新規GASのデプロイURLにブラウザでアクセスしてデータが返ってくる
- [ ] 新規GitHubリポジトリにソースコードが正常に移管されている
- [ ] 新規VercelでWebサイトがビルド成功（Status: Ready）になっている
- [ ] HP上で「お知らせ」「加盟店一覧」がスプレッドシートと連動して正常に表示される
- [ ] スマホで開いてハンバーガーメニューや加盟店リンク（Instagram / オークション / MEDAICHI）が正しく動作する
